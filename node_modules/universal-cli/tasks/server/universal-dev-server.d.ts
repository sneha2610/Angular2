export declare class UniversalDevServer {
    private webpackCompiler;
    private webpackDevServerConfiguration;
    private appPath;
    private nodemonConfig;
    private demon;
    private liveServer;
    constructor(webpackCompiler: any, webpackDevServerConfiguration: any);
    listen(port: number, host: string, callBack: any): void;
}
