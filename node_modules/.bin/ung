#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../universal-cli/bin/ung" "$@"
  ret=$?
else 
  node  "$basedir/../universal-cli/bin/ung" "$@"
  ret=$?
fi
exit $ret
